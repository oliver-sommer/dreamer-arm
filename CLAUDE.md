> **`./do-not-touch/` is an archived reference for a discarded implementation. Do not read it.**

The files inside `do-not-touch/` are the old arm-specific env + training-loop layer
that was removed. They are kept only so the full git history is intact and the
implementation decisions are visible to humans. A fresh reimplementation is in
progress; the archived code must NOT be read or used as a template.
